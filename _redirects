# Netlify Redirects Configuration
# Serve SPA fallback to index.html for client-side routing

# Allow static assets to 404
/wasm/*.js         200
/wasm/*.wasm       200
/wasm/*.epw        200
/wasm/*.epk        200
/wasm/*.html       200
/*.js              200
/*.css             200
/*.wasm            200
/*.epw             200
/*.epk             200
/lang/*            200

# Fallback to index.html for SPA routing
/*                 /index.html   200
